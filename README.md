# Living Teleprompter

An unplanned demo machine. The presenter speaks, and the audience sees a living page that keeps pace with the talk.

## Run

```bash
cd mvp
export OPENAI_API_KEY=sk-...
npm install
npm run build
npm run server
```
